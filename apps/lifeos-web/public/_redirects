# LifeOS API proxy (must be above SPA fallback)
/api/*  https://lucid-integrity-production.up.railway.app/:splat  200

# HospitalityOS business PWAs (before LifeOS SPA fallback)
/hos/*  /hos/index.html  200

# SPA fallback
/*    /index.html   200
