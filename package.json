{
  "name": "lifeos",
  "private": true,
  "version": "1.0.0",
  "description": "LifeOS V1 — Universal user shell for the TrustID ecosystem",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web,hos -c teal,cyan,magenta \"npm:dev:api\" \"npm:dev:web\" \"npm:dev:hospitality\"",
    "dev:api": "npm run dev -w @lifeos/api",
    "dev:web": "npm run dev -w @lifeos/web",
    "dev:hospitality": "npm run dev -w @lifeos/mock-hospitalityos",
    "build": "npm run build -w @lifeos/shared && npm run build -w @lifeos/auth-client && npm run build -w @lifeos/token-network && npm run build -w @lifeos/experience-sdk && npm run build -w @lifeos/ui && npm run build -w @lifeos/api && npm run build -w @lifeos/web && npm run build -w @lifeos/mock-hospitalityos",
    "db:generate": "npm run db:generate -w @lifeos/api",
    "db:push": "npm run db:push -w @lifeos/api",
    "db:seed": "npm run db:seed -w @lifeos/api",
    "icons": "node scripts/generate-icons.mjs",
    "setup": "npm install && npm run icons && npm run build -w @lifeos/shared && npm run build -w @lifeos/auth-client && npm run build -w @lifeos/token-network && npm run build -w @lifeos/experience-sdk && npm run build -w @lifeos/ui && npm run db:generate && npm run db:push && npm run db:seed",
    "test": "npm run test -w @lifeos/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
