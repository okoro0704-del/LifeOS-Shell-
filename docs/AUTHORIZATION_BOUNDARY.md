# Authorization Boundary

`AuthorizationProvider`:

- authorizeAction
- authorizePayment
- authorizeDevice

Mock implementation for development. TrustID remains the identity/security system — LifeOS does not implement TrustID auth, biometrics, or device trust itself.
